# Fonts

The favicon KB monogram is generated from **Iowan Old Style**, the same serif used on the site.

Regenerate after changing the favicon script:

```bash
npm run generate:favicon
```

On macOS, the script reads the system font from `/System/Library/Fonts/Supplemental/Iowan Old Style.ttc`.
